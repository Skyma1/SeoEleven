# SeoEleven - Digital Studio Website

Профессиональный сайт для B2B digital-студии, специализирующейся на SEO, разработке, дизайне и автоматизации.

## 🚀 Технологии

- **React 18.2.0** - UI библиотека
- **React Router 7.11.0** - Роутинг
- **CSS Modules** - Изолированные стили компонентов
- **Lucide React** - Иконки

## 📦 Установка и запуск

### Требования

- Node.js 16+
- npm или yarn

### Установка

```bash
npm install
```

### Запуск в режиме разработки

```bash
npm start
```

Приложение откроется на `http://localhost:3000`

### Сборка для production

```bash
npm run build
```

Собранные файлы будут в папке `build/`

## 📁 Структура проекта

```
src/
├── services/          # API клиент
│   └── api.js        # Централизованный API сервис
├── hooks/            # Custom React hooks
├── context/          # React Context (Data, Auth, Modal)
├── utils/            # Утилиты (валидация, форматирование, ошибки)
├── config/           # Константы приложения
├── components/       # React компоненты
├── admin/            # Админ-панель
├── data/             # Локальные данные (фоллбэк)
└── styles/           # CSS Modules
```

## 🎨 Дизайн-система

### Цвета

- **Фон страницы**: `#F0EEE9`
- **Фон карточек**: `#FFFFFF` или `#FAF9F6`
- **Акцент**: `#4F6F5E`
- **Текст основной**: `#2E2E2E`
- **Текст вторичный**: `#5F5F5F`

### Отступы

Система отступов: `4px / 8px / 12px / 16px / 24px / 32px / 48px / 64px`

### Скругления

- `--radius-xs`: 10px
- `--radius-s`: 14px
- `--radius-m`: 18px
- `--radius-l`: 24px

## 🔌 Интеграция с бекендом

### Текущее состояние

✅ **Фронтенд полностью готов к интеграции:**

- Централизованный API сервис (`/src/services/api.js`)
- React Context для глобального состояния
- Custom hooks для работы с данными
- Валидация форм и обработка ошибок
- Error Boundary для стабильности
- Защищенные роуты для админки
- Фоллбэк на локальные данные

### Для бекенд-разработчика

📖 **Читайте [BACKEND.md](BACKEND.md)** - полная инструкция по интеграции

**Быстрый старт:**

1. Создайте `.env` файл:
```bash
REACT_APP_API_URL=http://localhost:3001/api
```

2. Запустите бекенд на порту 3001

3. Перезапустите фронтенд: `npm start`

**Минимально необходимые эндпоинты:**
- `POST /api/contact` - Контактная форма
- `GET /api/blog` - Список постов
- `GET /api/cases` - Список кейсов
- `POST /api/admin/login` - Авторизация админа

Подробности в [BACKEND.md](BACKEND.md)

### Работа БЕЗ бекенда

Фронтенд работает в **dev режиме** без бекенда:

- ✅ Контактная форма показывает сообщение
- ✅ Блог и кейсы загружаются из локальных данных
- ✅ Админка доступна с любым email/паролем
- ✅ Все компоненты работают

Для входа в админку: `http://localhost:3000/admin/login`

## 🏗️ Архитектура

### API клиент

Все запросы идут через `/src/services/api.js`:

```javascript
import apiService from './services/api';

// Публичные API
const { data, error } = await apiService.getBlogPosts();
const result = await apiService.submitContactForm(formData);

// Админские API (требуют токен)
const posts = await apiService.adminGetBlogPosts();
const created = await apiService.adminCreateBlogPost(postData);
```

### React Context

- **DataContext** - управление данными (блог, кейсы)
- **AuthContext** - аутентификация администратора
- **ModalContext** - модальные окна

### Custom Hooks

- `useAsync` - универсальный хук для async операций
- `useBlogPost` - загрузка одного поста
- `useBlogPosts` - загрузка списка постов
- `useCases` - загрузка кейсов

Все хуки с автоматическим фоллбэком на локальные данные.

### Валидация

Централизованная валидация в `/src/utils/validation.js`:

```javascript
import { validateContactForm } from './utils/validation';

const { isValid, errors } = validateContactForm(formData);
```

## 🔐 Админ-панель

### Доступ

**URL:** `http://localhost:3000/admin/login`

**Dev режим (без бекенда):**
- Email: любой
- Password: любой

**Production (с бекендом):**
- Требуется реальная авторизация через API

### Функции админки

- 📝 Управление блогом (CRUD)
- 💼 Управление кейсами (CRUD)
- 📧 Просмотр заявок
- 📊 Статистика
- ⚙️ Управление услугами

## 🌐 Деплой

### Статический хостинг (Netlify, Vercel)

```bash
npm run build
```

Загрузите содержимое `build/` на хостинг.

**Переменные окружения на хостинге:**
```
REACT_APP_API_URL=https://api.yourdomain.com/api
```

### С бекендом (Node.js/Express)

```javascript
const express = require('express');
const path = require('path');
const app = express();

// API роуты
app.use('/api', apiRoutes);

// Отдача статики фронтенда
app.use(express.static(path.join(__dirname, 'build')));
app.get('*', (req, res) => {
  res.sendFile(path.join(__dirname, 'build', 'index.html'));
});
```

### Nginx

```nginx
server {
    listen 80;
    server_name yourdomain.com;

    location / {
        root /var/www/seoeleven/build;
        try_files $uri /index.html;
    }

    location /api {
        proxy_pass http://localhost:3001;
    }
}
```

## 📝 Основные компоненты

### Публичная часть

- **Header** - Шапка с навигацией и мега-меню
- **Hero** - Главный блок (bento layout)
- **Services** - Секция услуг (bento grid)
- **Quiz** - Калькулятор стоимости
- **CTA** - Призыв к действию
- **Footer** - Подвал
- **ContactModal** - Модальная форма заявки

### Страницы услуг

- SEO-продвижение
- GEO / AI SEO
- Разработка сайтов
- Автоматизация
- Реклама
- И другие...

### Админ-панель

- **AdminLayout** - Общий layout с сайдбаром
- **AdminLogin** - Страница входа
- **AdminBlog** - Управление блогом
- **AdminCases** - Управление кейсами
- **AdminRequests** - Просмотр заявок
- **AdminStatistics** - Статистика

## 🛠️ Утилиты

### Валидация (`/src/utils/validation.js`)

- Валидация контактной формы
- Валидация логина
- Валидация постов блога
- Валидация кейсов

### Форматирование (`/src/utils/formatters.js`)

- Форматирование дат
- Форматирование чисел
- Форматирование валюты
- Форматирование текста

### Обработка ошибок (`/src/utils/errorHandlers.js`)

- Получение сообщений об ошибках
- Определение типов ошибок
- Обработка ошибок API
- Парсинг ошибок форм

## 🔧 Конфигурация

### Переменные окружения

Создайте `.env` файл:

```bash
REACT_APP_API_URL=http://localhost:3001/api
REACT_APP_ENV=development
```

### Константы

Все константы в `/src/config/constants.js`:

- Списки услуг
- Цели и бюджеты
- Статусы заявок
- Категории блога
- URL роутов
- И другие...

## 🧪 Тестирование

### Проверка сборки

```bash
npm run build
```

### Проверка линтера

Все файлы проверены ESLint - 0 ошибок.

### Проверка API интеграции

1. Запустите фронтенд: `npm start`
2. Откройте DevTools → Network
3. Отправьте форму или войдите в админку
4. Проверьте запросы к API

## 📚 Документация

- **README.md** (этот файл) - Общая информация
- **BACKEND.md** - Интеграция с бекендом
- **src/data/README.md** - Структура данных

## 🐛 Troubleshooting

### "Failed to fetch" при входе в админку

**Причина:** Бекенд не запущен

**Решение:** 
- В dev режиме работает без бекенда (любой email/пароль)
- Для production создайте `.env` и запустите бекенд

### CORS ошибка

**Решение:** На бекенде добавьте:
```javascript
app.use(cors({ origin: 'http://localhost:3000' }));
```

### Не загружаются данные блога/кейсов

**Решение:** 
- Проверьте что API возвращает правильный формат
- В dev режиме используются локальные данные из `/src/data/`

## 📄 Лицензия

Private project

## 👥 Контакты

Для вопросов по интеграции читайте [BACKEND.md](BACKEND.md)
