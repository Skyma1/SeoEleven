# SeoEleven3 - Студийный сайт

Профессиональный одностраничный сайт для B2B студии, специализирующейся на SEO, разработке, дизайне и автоматизации.

## 🚀 Технологии

- **React 18.2.0** - UI библиотека
- **CSS Modules** - изолированные стили компонентов
- **Lucide React** - иконки (outline, 24px, stroke 1.5)
- **Create React App** - сборка проекта

## 📦 Установка и запуск

### Требования

- Node.js 16+ 
- npm или yarn

### Установка зависимостей

```bash
npm install
```

### Запуск в режиме разработки

```bash
npm start
```

Приложение откроется на `http://localhost:3000`

### Сборка для production

```bash
npm run build
```

Собранные файлы будут в папке `build/`, готовые для деплоя на любой статический хостинг (Netlify, Vercel, GitHub Pages и т.д.)

## 📁 Структура проекта

```
src/
├── components/          # React компоненты
│   ├── Header.jsx       # Шапка сайта
│   ├── Hero.jsx         # Главный блок (bento layout)
│   ├── Services.jsx    # Секция услуг (bento grid)
│   ├── CTA.jsx          # Призыв к действию
│   └── Footer.jsx       # Подвал
├── styles/              # CSS модули
│   ├── globals.css      # Глобальные стили и дизайн-система
│   ├── Header.module.css
│   ├── Hero.module.css
│   ├── Services.module.css
│   ├── CTA.module.css
│   └── Footer.module.css
├── App.jsx              # Корневой компонент
└── index.jsx            # Точка входа
```

## 🎨 Дизайн-система

### Цвета

- **Фон страницы**: `#F0EEE9`
- **Фон карточек**: `#FFFFFF` или `#FAF9F6`
- **Акцент**: `#4F6F5E`
- **Текст основной**: `#2E2E2E`
- **Текст вторичный**: `#5F5F5F`
- **Границы**: `rgba(0, 0, 0, 0.06)`

### Отступы

Система отступов: `4px / 8px / 12px / 16px / 24px / 32px / 48px / 64px`

### Скругления

- `--radius-xs`: 10px
- `--radius-s`: 14px
- `--radius-m`: 18px
- `--radius-l`: 24px

### Анимации

- `--transition-fast`: 200ms ease
- `--transition-base`: 300ms ease
- `--transition-slow`: 500ms ease

## 🏗️ Архитектура

### Grid Layout

Проект использует строгую grid-систему:

- **Hero Section**: 12-колоночная сетка
  - Доминирующий блок: 8 колонок × 2 строки
  - Вторичные блоки: по 4 колонки каждый

- **Services Section**: 12-колоночная сетка
  - Доминирующий блок: 7 колонок
  - Вторичный большой: 5 колонок
  - Средние блоки: по 6 колонок
  - Малые блоки: по 3 колонки

Все блоки заполняют контейнер полностью, без пустых областей.

### Компоненты

Все компоненты используют CSS Modules для изоляции стилей. Каждый компонент самодостаточен и может быть легко переиспользован.

## 🔌 Интеграция с бекендом

### Текущее состояние

Сайт полностью статический, все данные захардкожены в компонентах.

### Рекомендации для интеграции

#### 1. API эндпоинты

Рекомендуемые эндпоинты для получения данных:

```
GET /api/services          # Список услуг
GET /api/contact           # Контактная информация
GET /api/social            # Социальные сети
```

#### 2. Структура данных

**Услуги** (`/api/services`):
```json
{
  "services": [
    {
      "id": "seo",
      "title": "SEO и поисковая оптимизация",
      "description": "Стратегическая оптимизация...",
      "icon": "Search",
      "link": "#seo"
    }
  ]
}
```

**Контактная информация** (`/api/contact`):
```json
{
  "email": "hello@studio.com",
  "phone": "+1 (234) 567-890",
  "address": "..."
}
```

#### 3. Где добавить API вызовы

**Вариант 1: Context API (рекомендуется)**

Создать `src/context/DataContext.jsx`:
```jsx
import { createContext, useContext, useState, useEffect } from 'react';

const DataContext = createContext();

export const useData = () => useContext(DataContext);

export const DataProvider = ({ children }) => {
  const [services, setServices] = useState([]);
  const [contact, setContact] = useState({});
  
  useEffect(() => {
    // Загрузка данных
    fetch('/api/services')
      .then(res => res.json())
      .then(setServices);
      
    fetch('/api/contact')
      .then(res => res.json())
      .then(setContact);
  }, []);
  
  return (
    <DataContext.Provider value={{ services, contact }}>
      {children}
    </DataContext.Provider>
  );
};
```

**Вариант 2: Прямые fetch в компонентах**

Добавить `useEffect` в каждый компонент, который нуждается в данных.

#### 4. Обработка состояний загрузки

Добавить состояния loading/error в компоненты:

```jsx
const [loading, setLoading] = useState(true);
const [error, setError] = useState(null);
const [data, setData] = useState([]);
```

#### 5. Формы обратной связи

Для CTA секции можно добавить форму:

```jsx
// В CTA.jsx
const handleSubmit = async (e) => {
  e.preventDefault();
  const response = await fetch('/api/contact', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(formData)
  });
};
```

### Переменные окружения

Создать `.env` файл для API URL:

```env
REACT_APP_API_URL=http://localhost:3001/api
```

Использовать в коде:
```jsx
const API_URL = process.env.REACT_APP_API_URL;
```

## 🌐 Деплой

### Статический хостинг

Проект готов к деплою на любой статический хостинг:

- **Netlify**: подключить GitHub репозиторий, автоматический деплой
- **Vercel**: `vercel` в корне проекта
- **GitHub Pages**: использовать `gh-pages` пакет

### С бекендом

Если бекенд на том же домене:
1. Собрать проект: `npm run build`
2. Скопировать содержимое `build/` в папку статики бекенда
3. Настроить роутинг (все запросы → `index.html` для SPA)

## 📝 Язык контента

Весь контент на русском языке. Для смены языка потребуется:
1. Вынести все тексты в отдельный файл локализации
2. Использовать библиотеку i18n (например, `react-i18next`)

## 🐛 Известные ограничения

- Нет роутинга (одностраничный сайт)
- Нет обработки форм
- Нет интеграции с API
- Нет управления состоянием (Redux/Zustand)

## 📄 Лицензия

Private project

## 👥 Контакты

Для вопросов по интеграции обращайтесь к команде разработки.
